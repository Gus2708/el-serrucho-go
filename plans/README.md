# Implementation Plans

Generated by the `improve` skill on 2026-06-22 against commit `75ad0a7`.

Execute in the order below unless dependency notes say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

---

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](001-typecheck-and-lint-scripts.md) | Add `typecheck` and `lint` scripts | P1 | S | — | DONE — branch `advisor/001-typecheck-lint-scripts`. Note: lint baseline 1156 warnings (0 errors); `--max-warnings 9999` used. |
| [002](002-extract-pdf-upload-helper.md) | Extract PDF upload helper | P1 | S | — | DONE — branch `advisor/002-extract-pdf-upload-helper`. |
| [003](003-sync-status-select-actualizado-en.md) | Type and document narrow selects in `useSyncStatus` | P2 | S | — | DONE — branch `advisor/003-sync-status-typed-selects`. |
| [004](004-vercel-index-cache-control.md) | Add `Cache-Control` for `index.html` in Vercel | P2 | S | — | DONE — branch `advisor/004-vercel-index-cache`. |
| [005](005-pdf-upload-failure-throws.md) | PDF upload failure throws instead of silently returning null | P1 | M | 002 | DONE — branch `advisor/005-pdf-upload-throw-on-failure`. |
| [006](006-atomic-order-submission.md) | Atomic order/presupuesto submission (compensating delete) | P1 | M | — | DONE — branch `advisor/006-atomic-order-submission`. |
| [007](007-pdf-generator-refactor.md) | Refactor `pdfGenerator.ts` — extract shared document shell | P2 | M | 001 | DONE — branch `advisor/007-pdf-generator-refactor`. File: 721→710 lines. |
| [008](008-role-cache-ttl.md) | Add TTL to offline role cache in `useUserRole` | P2 | M | — | DONE — branch `advisor/008-role-cache-ttl`. |
| [009](009-persist-draft-order.md) | Persist draft change order across PWA reloads | P3 | S | — | TODO |
| [010](010-margin-warning-on-quote-price.md) | Warn when quote price is below cost in presupuesto | P3 | M | — | TODO |
| [011](011-test-suite-bootstrap.md) | Bootstrap minimal test suite (pdfStorage + pdfGenerator) | P3 | L | 001 | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED (reason)` | `REJECTED (reason)`

---

## Dependency notes

- **005 requires 002**: `uploadPdfAndGetUrl` must exist in `src/lib/pdfStorage.ts` before plan 005 changes its behavior.
- **007 requires 001**: uses `npm run typecheck` as verification gate at every step.
- **011 requires 001**: uses `npm test` script; also benefits from typecheck being in place.
- **003, 004, 006, 008, 009, 010** are independent — execute in any order.

---

## Suggested execution batches

If parallelizing across agents:

**Batch A** (no deps, all S-effort): 001, 002, 003, 004 — run these first or in parallel.

**Batch B** (after A): 005 (needs 002), 006, 007 (needs 001), 008 — run these after their deps land.

**Batch C** (lowest urgency): 009, 010, 011 — direction/improvement plans, no blockers on previous plans.

---

## Findings considered and rejected

These were audited but rejected after vetting the live code. Recorded so they are not re-flagged in future runs.

| Finding | Reason |
|---------|--------|
| BUG-03: Stale closure in `useSessionEnforcer` dep array | Dependency array already includes `session?.access_token` — effect re-runs on token refresh. Not a bug. |
| PERF-04: `FlashList` on ventas missing `estimatedItemSize` | Already has `estimatedItemSize={114}` at `app/(tabs)/ventas.tsx:208`. Subagent error. |
| PERF-03: Search summary N+1 counts | Already uses `{ count: 'exact', head: true }` — fast by design. |
| SEC-05: CORS wildcard on `manifest.webmanifest` | Web manifests are public resources; `Access-Control-Allow-Origin: *` is standard practice here. Not a security issue. |
| PERF-09: `renderItem` / `keyExtractor` recreation | Subagent concluded this is a non-issue; both are already memoized. |
| PERF-05: Realtime subscription lifecycle | Subagent concluded cleanup is correct (`cancelled = true` + `supabase.removeChannel`). |

---

## Lint baseline

After Plan 001 lands, record the initial warning count here:

> **Baseline warnings**: _fill in after `npm run lint 2>&1 | tail -3` runs_

Future cleanup plans should track reduction from this number.
